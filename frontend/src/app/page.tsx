import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function HomePage() {
  return (
    <PlaceholderPage
      title="Eratex Operating Spine"
      description="Order lifecycle, procurement readiness, fabric QC, PCD gates, and shared platform services are ready for later planning modules."
    />
  );
}
